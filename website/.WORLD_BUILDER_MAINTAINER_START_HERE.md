# Dragonwilds Sync World Builder — Maintainer Start Here

This file is intentionally hidden from the public website navigation.

Before changing the browser World Builder, `.rsdwl` Website Draft packaging/import, World-save attachment, Community Rules, mod-family presentation, Desktop profile compatibility, WebGUI authoring, public placards, platform/store compatibility, or first-launch behavior, read these files completely:

1. `website/.WEB_BUILDER_RSDWL_COMPATIBILITY.md`
   - `.rsdwl` v3 Website Draft trust model
   - browser-local packaging
   - optional World-save payload
   - safe desktop import/staging
   - fresh local credential generation
   - no browser/admin authority

2. `website/.WORLD_DRAFT_FIRST_LAUNCH_CONTRACT.md`
   - Website Draft lifecycle state
   - visible DRAFT status after import
   - Finish World Setup wizard
   - Start Server / Launch interception while setup is incomplete
   - runtime/save/network/security/publication setup order
   - resumable setup journal and recovery behavior
   - exact completion semantics before a World becomes runnable

3. `website/.WORLD_BUILDER_RULES_MODS_APPLICATION_CONTRACT.md`
   - Community Rules presets and editable final text
   - Normal / Kids / Adults / Roleplay / Custom IDs
   - clickable Rules detail chips
   - UE4SS / RuneSchema / PAKs mod families
   - Desktop/WebGUI/public metadata mapping
   - legacy compatibility rules

4. `website/.WORLD_BUILDER_PLATFORM_STORE_COMPATIBILITY.md`
   - canonical platform/store registry
   - Nintendo Switch 2 canonical key and baked asset
   - legacy `nintendo` -> `switch2` import normalization
   - full-color platform SVG requirements
   - Desktop/WebGUI/website parity
   - trusted Dragonwilds storefront URLs
   - footer store-icon behavior
   - Website Draft platform compatibility rules

5. `resources/world-builder-presets.json`
   - canonical built-in Rules preset text
   - canonical mod-family IDs and labels

6. `resources/platform-registry.json`
   - canonical platform keys, aliases and display labels
   - canonical baked asset filenames
   - trusted Dragonwilds storefront destinations
   - legacy platform normalization rules

Canonical bundled ecosystem/platform artwork includes:

```text
renderer/assets/platforms/steam.svg
renderer/assets/platforms/windows.svg
renderer/assets/platforms/xbox.svg
renderer/assets/platforms/playstation.svg
renderer/assets/platforms/switch2.svg
renderer/assets/platforms/epicgames.svg
renderer/assets/platforms/ue4ss.svg
renderer/assets/platforms/runeschema.svg
renderer/assets/platforms/paks.svg
```

Current public website entrypoint:

```text
website/world-builder.html
```

Current browser implementation:

```text
website/home-demo.js
website/home-demo.css
website/world-builder-controls.css
website/world-draft-onboarding.js
website/platform-assets.js
website/placards.js
website/placard-detail-popovers.css
```

## Mandatory compatibility instruction

Do not create a second incompatible World Builder schema.

The browser produces an **untrusted Website Draft `.rsdwl`**. The desktop application validates it through a dedicated restricted import path, creates a new local World profile and fresh secrets, stages any included World save safely, and creates that World in **DRAFT / setup-required** state.

A Website Draft is visible and editable after import, but it is not runnable yet. Opening it for setup, clicking `Finish Setup`, clicking `Start Server`, or first-launching it must open/resume the same persisted **Finish World Setup** wizard. Only after required local runtime, save, networking, credential, publication, and security checks pass does the application change the draft to `ready` and allow normal runtime control.

Rules and Mods are shared structured World metadata:

```text
community_rule_profile
community_rules (legacy compatibility projection)
mod_groups.ue4ss
mod_groups.runeschema
mod_groups.paks
```

Platform compatibility is shared structured metadata too. New application/WebGUI work should normalize Nintendo compatibility to:

```text
switch2
```

while continuing to read legacy Website Draft/profile values such as:

```text
nintendo
nintendo switch
switch
```

as Nintendo Switch 2.

Desktop, WebGUI, public descriptors, website placards, focused placards, `.rsdwl` import/export, and storefront actions must converge on the normalized platform registry rather than maintaining separate copies or arbitrary URLs.
