# RC2 build candidate

This marker identifies the hands-on testing candidate after the RC2 feedback pass.

The candidate includes persistent World deletion tombstones, conservative WebHost/remote defaults, faster WebHost shutdown, Community subscriptions, visible remote-user permission management, colored platform/community/OS marks, default-router detection, GitHub-only changelog presentation, retired active Defender integration, lighter in-app navigation instead of full-app detached Mod Management windows, immediate sidebar collapse feedback, and protected launcher-owned RSDWTools runtime support.

Do not promote this branch to `main` until hands-on Windows and Ubuntu testing is accepted.
