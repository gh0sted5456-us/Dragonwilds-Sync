{
  "name": "dragonwilds-sync-world-directory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node test-retention.cjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:create": "wrangler d1 create dragonwilds-sync-worlds",
    "db:migrate:local": "wrangler d1 migrations apply dragonwilds-sync-worlds --local",
    "db:migrate:remote": "wrangler d1 migrations apply dragonwilds-sync-worlds --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
